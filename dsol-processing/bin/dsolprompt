#!/usr/bin/bash
# -*- mode: shell-script -*-

#DIR=$(pwd)
LENGTH="50"
HALF="$((${LENGTH}/2))"

if [ ${#PWD} -gt $((${LENGTH})) ]
then
    echo "${PWD:0:$((${HALF}-3))}...${PWD:$((${#PWD}-$HALF)):$HALF}" | \
        sed "s|^$HOME|~|"
else
    echo "${PWD}" | sed "s|^$HOME|~|"
fi
