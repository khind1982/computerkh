#!/bin/bash
# -*- mode: shell-script -*-

# A thin wrapper around the xmlise binary.
#
# Since we use both i386 and SPARC machines, we need to 
# test which platform we are running on so that we can
# ensure we run the correct binary for the given platform.

$(dirname $0)/../libexec/lion/xmlise.`uname -p` $@